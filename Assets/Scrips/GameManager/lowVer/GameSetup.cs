using UnityEngine;

public class GameSetup : MonoBehaviour
{
    //[Header("References")]
    //[SerializeField] private PlayerWeaponManager playerWeaponManager;
    //[SerializeField] private Transform playerTransform;

    //[Header("Test Gun Prefabs")]
    //[SerializeField] private BaseGun riflePrefab;
    //[SerializeField] private BaseGun sniperPrefab;
    //[SerializeField] private BaseGun grenadeLauncherPrefab;

    //void Start()
    //{
    //    // Spawn Rifle
    //    if (riflePrefab != null)
    //    {
    //        BaseGun rifle = Instantiate(riflePrefab, playerTransform.position, Quaternion.identity, playerTransform);
    //        rifle.SetTargetingStrategy(new ClosestEnemyStrategy());
    //        playerWeaponManager.AddWeapon(rifle);
    //        Debug.Log("add rifle");

    //    }

    //    // Spawn Sniper
    //    if (sniperPrefab != null)
    //    {
    //        BaseGun sniper = Instantiate(sniperPrefab, playerTransform.position, Quaternion.identity, playerTransform);
    //        sniper.SetTargetingStrategy(new HighestHealthEnemyStrategy());
    //        playerWeaponManager.AddWeapon(sniper);
    //        Debug.Log("add sniper");
    //    }

    //    // Spawn Grenade Launcher
    //    if (grenadeLauncherPrefab != null)
    //    {
    //        BaseGun grenade = Instantiate(grenadeLauncherPrefab, playerTransform.position, Quaternion.identity, playerTransform);
    //        grenade.SetTargetingStrategy(new ClusteredEnemyStrategy());
    //        playerWeaponManager.AddWeapon(grenade);
    //        Debug.Log("add grenade");
    //    }
    //}
}
